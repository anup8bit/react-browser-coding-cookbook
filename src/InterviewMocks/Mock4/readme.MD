Problem:
Build a custom infinite scroll component in React without using external libraries. Requirements:

Fetch paginated API data (simulate with setTimeout).

Show loader when fetching.

Handle edge cases (no more data, error state).

Optimize re-renders.