https://chatgpt.com/share/68c58bf8-26b0-8001-93c2-b5d7e24a4fc1

Section 1 – Algorithm / DSA

Q1. Given an array of integers, return all unique triplets [a, b, c] such that a + b + c = 0.

Section 2 – Frontend Browser Coding (React)

Q2. Implement a Tag Input component in React.

User can type text and press Enter to add a tag.

Tags show with a close x icon to remove.

Duplicate tags not allowed.

Controlled input with state.

CSS: simple pill-style tags.

Section 3 – JavaScript Polyfill

Q3. Write a polyfill for Array.prototype.map.

Section 4 – Arrays (JS)

Q4. Flatten an array:

const arr = [1, [2, [3, [4]], 5]];
// output -> [1,2,3,4,5]


Q5. Find the first non-repeating element in an array.

Section 5 – Strings (JS)

Q6. Reverse words in a string.
Q7. Check if two strings are anagrams (ignore spaces, case).

Section 6 – Promises

Q8. Implement Promise.all polyfill.

Q9. What is the output order?
Promise.resolve()
  .then(() => console.log(1))
  .then(() => console.log(2));

Promise.resolve()
  .then(() => console.log(3))
  .then(() => console.log(4));
